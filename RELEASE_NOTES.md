# 时间便签 0.1.1 · macOS 公开预览版

每日清单里的已有任务，现在可以直接放进长期目标，不用再创建一遍。

## 这次更新

- 每日任务“…”增加“加入长期目标”；已有归属时可调整目标或移出。
- 长期目标增加“加入已有工作”，支持按名称、分类或日期筛选，保留原来的“安排工作”。
- 每日清单显示任务所属目标，两处仍是同一项任务，不复制、不重复统计。
- 过去或已经记录的任务也可补充归属；只变选中项，不改日期、时段、进度和历史，也不自动改变重复系列。
- 保存前显示归属变化；取消不保存，过时操作拒绝覆盖。移出目标不删除每日工作。
- 主项目正式采用 **MIT 许可证**，版权署名 **Kengo Kubota 和 Keng0nion**。全文见仓库 `LICENSE` 与应用设置中的“时间便签 MIT 许可”；两个第三方原 MIT 许可完整保留。

## 下载

- **推荐 DMG**：`TimeNote-0.1.1-macOS-arm64.dmg`，打开后拖入“应用程序”。
- **也可 ZIP**：`TimeNote-0.1.1-macOS-arm64.zip`，解压即为同一应用。
- `SHA256SUMS.txt` 用于核对两份下载文件。
- 仅 Apple Silicon（M 系列），**不支持 Intel**。构建目标 macOS 14.0，仅在 macOS 26.6.2 / Apple Silicon 实测。

## 升级与数据

先在旧版导出备份，再完全退出旧程序，然后打开新版；不要同时运行两份。0.1.0 Release 保留，此版沿用原生数据位置，不需要搬数据库，不含示例工作。

数据库结构与原生 JSON 版本1不变。关联前的旧备份若含同编号、不同归属的工作，导入会因冲突停止，而不是覆盖新归属。请保留旧备份，并在整理后导出最新备份。网页 Demo 备份仍不支持导入。

## 使用前请注意

1. **这是预览版。严格提醒未启用，睡眠或完全退出后的持续响铃尚未实现、未验证；不要用它代替重要闹钟。**
2. **没有 Apple 开发者签名或公证。** 临时签名只检查完整性，下载后可能被 macOS 阻止。请保留提示并反馈，不要关闭 Gatekeeper、移除隔离属性或改安全策略。
3. 没有云同步、自动上传、AI 或自动更新。数据库和备份未额外加密，请妥善保管。
4. 全局快捷键跨应用实际触发、系统备份对话框完整操作、其他系统／机器和长期压力尚未验收。
5. 本机两种窗口流程通过，但仍出现 SwiftUI 布局警告及部分系统输入法日志；尚未在其他环境验证。
6. MIT允许修改、商用和分发，但须保留版权及许可声明，按原样提供、无担保；完整条款以 `LICENSE` 为准。

使用说明与反馈入口见仓库 README。请勿把自己的计划、数据库、备份或私人路径放入公开 Issue。
