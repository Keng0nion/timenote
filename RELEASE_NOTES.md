# 时间便签 0.1.0 · macOS 公开预览版

第一版可下载的原生“时间便签”：用每天的起止时段安排工作，在本机记录进展，逐渐看到长期积累。

## 下载

- **推荐 DMG**：`TimeNote-0.1.0-macOS-arm64.dmg`，打开后拖入“应用程序”。
- **也可 ZIP**：`TimeNote-0.1.0-macOS-arm64.zip`，解压即为同一应用。
- `SHA256SUMS.txt` 用于核对两份下载文件。
- 仅 Apple Silicon（M 系列），**不支持 Intel**。构建目标 macOS 14.0，仅在 macOS 26.6.2 / Apple Silicon 实测。

## 已可使用

- “每天几点到几点”的工作安排、跨午夜、每天／工作日／自选周几重复。
- 手动拆分长期目标，勾选完成或记录百分比与备注。
- 追加历史、单步撤销；未来安排先看差异再确认修改。
- 日历、趋势、上周分类与上月整体回顾。
- 本机 SQLite 保存、JSON 导出与预览后合并导入。
- 菜单栏入口、应用内快捷键、可自行设置的全局快捷键。
- 已接入 GRDB.swift 和 KeyboardShortcuts；Defaults 暂缓，少量偏好用系统存储。

## 使用前请注意

1. **这是预览版。严格提醒未启用，睡眠或完全退出后的持续响铃尚未实现、未验证；不要用它代替重要闹钟。**
2. **没有 Apple 开发者签名或公证。** 临时签名只检查完整性，下载后可能被 macOS 阻止。请保留提示并反馈，不要关闭 Gatekeeper、移除隔离属性或改安全策略。
3. 没有云同步、自动上传、AI 或自动更新。数据库和备份未额外加密，请妥善保管。
4. 全局快捷键跨应用实际触发、系统备份对话框完整操作、其他系统／机器和长期压力尚未验收。
5. 已用本机 0.1 版的用户：先导出备份、完全退出旧程序，再打开新版本；不要同时运行两份。沿用原生数据目录，不自动读取网页 Demo 备份。
6. 主项目最终开源许可证尚未选择，本次公开源码不代表已授予自由再分发许可；两个第三方原 MIT 许可完整保留。

使用说明与反馈入口见仓库 README。请勿把自己的计划、数据库、备份或私人路径放入公开 Issue。
