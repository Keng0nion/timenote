# 时间便签 0.1.3 · macOS 公开预览版

新增菜单栏状态面板，并附独立提醒验证工具；正式应用的提醒边界与 0.1.2 相同。

## 这次更新

- **菜单栏状态面板**：左键点击菜单栏图标弹出瞬态面板，显示当前进行中的工作、计划倒计时、快速记录，以及接下来与待补记录的摘要；右键仍弹原有菜单。倒计时展示计划时长，不是实际计时。
- **“今天”页改为驾驶舱分组**：进行中、接下来、待补记录分区显示，昨日跨午夜溢出安排进入今天候选；查看其他日期时仍按完整清单显示。
- 新增独立**提醒验证工具**（Tools/AlarmProbe/）：供开发验证 macOS 到点发声证据之用，不随正式应用打包、不连接正式数据库、不启动或退出正式软件、不改变进度。
- README 新增“接下来的改进顺序与 AI 边界”章节，说明后续推进顺序与 AI 接入边界。
- 构建修复：仓库位于非 ASCII 路径时 swift-driver 可能崩溃，构建临时目录改经纯 ASCII 别名；新增 CockpitChecks 纳入数据检查；命令行工具默认 SDK 不含 @State 宏插件，构建前用 SDKROOT 指向兼容 SDK。
- 主项目继续采用 **MIT 许可证**，版权署名 **Kengo Kubota 和 Keng0nion**。

## 下载

- **推荐 DMG**：`TimeNote-0.1.3-macOS-arm64.dmg`，打开后拖入“应用程序”。
- **也可 ZIP**：`TimeNote-0.1.3-macOS-arm64.zip`，解压即为同一应用。
- `SHA256SUMS.txt` 用于核对两份下载文件。
- 仅 Apple Silicon（M 系列），**不支持 Intel**。构建目标 macOS 14.0，仅在 macOS 27.0 / Apple Silicon 实测。

## 升级与数据

先在旧版导出备份，再完全退出旧程序，然后打开新版；不要同时运行两份。0.1.0与0.1.1与0.1.2 Release保留，此版沿用原生数据位置，不需要搬数据库，不含示例工作。

数据库结构与原生JSON版本1不变，0.1.3 没有升级数据库结构或原生 JSON 版本，也不会在升级时擅自整理旧版零散的目标归属。

## 使用前请注意

1. **这是预览版。严格提醒未启用，睡眠或完全退出后的持续响铃尚未实现、未验证；不要用它代替重要闹钟。**
2. **没有 Apple 开发者签名或公证。** 临时签名只检查完整性，下载后可能被macOS阻止。请保留提示并反馈，不要关闭Gatekeeper、移除隔离属性或改安全策略。
3. 没有云同步、自动上传、AI或自动更新。数据库和备份未额外加密，请妥善保管。
4. 菜单栏状态面板的计划倒计时不是实际计时；本版窗口与控件流程检查在 macOS 27.0 实机通过，其他能力边界与 0.1.2 相同，详见仓库README。

使用说明与反馈入口见仓库README。请勿把自己的计划、数据库、备份或私人路径放入公开Issue。

---

# 时间便签 0.1.2 · macOS 公开预览版

每日重复工作现在只显示一次，作为长期目标下的**持续行动**；每天的执行仍保留独立记录。

## 这次更新

- “加入已有工作”按原来的重复安排分组。例如30天学习和30天阅读，只显示两项，而不是60行日期；名称、分类或某天日期都可搜索整组。
- 目标内部显示“推进目标的行动”，包含日期范围、时段和截至今天的完成／记录天数。“每日记录”返回今日或最近安排的清单；未来日期不算漏记，不自动推算整个目标的完成率。
- **加入、换目标和移出统一处理整组已存在的日期，含过去、今天和未来。** 每日任务的“…”入口也按整组处理；保存前显示旧归属、新归属和受影响次数。
- 不复制工作，不改每日时间、进度、备注和历史；单次任务仍各自独立，同名但不同重复安排不会误合并，未生成的未来日期不会自动新增。
- 0.1.1留下的部分日期归属不会在升级时自动重写。选择窗口仍可选这组，目标页会提示已关联次数；确认后才整组整理。原本属于其他目标的日期也会移动，保存前请核对归属预览。
- 取消不保存；确认期间整组成员或内容已变化，会整组拒绝，避免覆盖新记录。
- 主项目继续采用 **MIT 许可证**，版权署名 **Kengo Kubota 和 Keng0nion**；应用设置可查看全文，两个第三方原MIT许可完整保留。

## 下载

- **推荐 DMG**：`TimeNote-0.1.2-macOS-arm64.dmg`，打开后拖入“应用程序”。
- **也可 ZIP**：`TimeNote-0.1.2-macOS-arm64.zip`，解压即为同一应用。
- `SHA256SUMS.txt` 用于核对两份下载文件。
- 仅 Apple Silicon（M 系列），**不支持 Intel**。构建目标 macOS 14.0，仅在 macOS 26.6.2 / Apple Silicon 实测。

## 升级与数据

先在旧版导出备份，再完全退出旧程序，然后打开新版；不要同时运行两份。0.1.0与0.1.1 Release保留，此版沿用原生数据位置，不需要搬数据库，不含示例工作。

数据库结构与原生JSON版本1不变。关联前的旧备份若含同编号、不同归属的工作，导入会因冲突停止，而不是覆盖新归属。请保留旧备份，并在整理后导出最新备份。网页Demo备份仍不支持导入。

## 使用前请注意

1. **这是预览版。严格提醒未启用，睡眠或完全退出后的持续响铃尚未实现、未验证；不要用它代替重要闹钟。**
2. **没有 Apple 开发者签名或公证。** 临时签名只检查完整性，下载后可能被macOS阻止。请保留提示并反馈，不要关闭Gatekeeper、移除隔离属性或改安全策略。
3. 没有云同步、自动上传、AI或自动更新。数据库和备份未额外加密，请妥善保管。
4. 全局快捷键跨应用实际触发、系统备份对话框完整操作、其他系统／机器和长期压力尚未验收。
5. 原生窗口检查方法和实际结果见仓库README；已知SwiftUI布局警告及部分系统输入法日志仍保留说明，不宣称运行日志无警告。
6. MIT允许修改、商用和分发，但须保留版权及许可声明，按原样提供、无担保；完整条款以`LICENSE`为准。

使用说明与反馈入口见仓库README。请勿把自己的计划、数据库、备份或私人路径放入公开Issue。
